    <p class="crumb">
<% if session[:admin_id].nil? %>
      <a href="/sites/login">[ログイン]</a>　
<% else %>
      <a href="/sites/logout"><%= session[:admin_name] %> [ログアウト]</a>　
<% end %>
      <a href="/">管理画面TOP</a>
<% if !@parent.nil? %>
  <% @parent.each do |href, page| %>
> <a href="<%= href %>"><%= page %></a>
  <% end %>
<% end %>

<% if !@mypage.nil? %>
> <%= @mypage %>
<% end %>
    </p>
